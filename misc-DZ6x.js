const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  images: {
    unoptimized: true,
    domains: ['images.unsplash.com']
  }
};

module.exports = {
  output: 'export',
  images: {
    unoptimized: true,
    domains: ['images.unsplash.com']
  }
};